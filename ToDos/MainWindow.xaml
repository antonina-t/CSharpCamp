<Window x:Class="ToDos.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ToDos"
        mc:Ignorable="d"
        Title="TODOs" Height="450" Width="800">
    <Grid x:Name="grid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <TextBox x:Name="textBox" Grid.Row="0" Margin="10" FontSize="20" Height="40" KeyUp="textBox_KeyUp"/>
        <Button x:Name="add" Grid.Row="0" Grid.Column="1" Height="40" Margin="10,0" Click="add_Click">Add TODO</Button>
        <ListBox x:Name="listBox" Grid.Row="1" Margin="10" FontSize="20"></ListBox>
        <Button x:Name="remove" Grid.Row="1" Grid.Column="1" Height="40" Margin="10,0" Click="remove_Click">Remove TODO</Button>

    </Grid>
</Window>
